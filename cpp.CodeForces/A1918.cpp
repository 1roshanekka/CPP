#include<bits/stdc++.h>
    using namespace std;
    
    void solve() {

        int n,m;
        cin>>n>>m;
        cout << (m/2)*n<<endl;
        //pick horizontal m
        //keep minimum so 1x2 ; 
        // if(m%2==0){ // even
        //     cout << (m/2)*n<<endl;
        // }
        // else{ // odd

        // }
    }

    signed main() {
        ios::sync_with_stdio(0);
        cin.tie(0);
        cout.tie(0);

            int _t;
            cin >> _t;
            while(_t--) {
                solve();
            }
    }